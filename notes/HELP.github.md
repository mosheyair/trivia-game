# 🚀 פקודות Git לעדכון פרויקט React ב־VS Code

מדריך קצר וברור לשימוש יומיומי ב־Git דרך הטרמינל של VS Code 👇

---

## 🧭 שלב 1 – בדוק שהפרויקט מחובר לגיטהאב

```bash
git remote -v
```

אם מופיע:

```
origin  https://github.com/USERNAME/REPO-NAME.git (fetch)
origin  https://github.com/USERNAME/REPO-NAME.git (push)
```

הכול תקין ✅

אם לא מופיע כלום:

```bash
git remote add origin https://github.com/USERNAME/REPO-NAME.git
```

---

## 🪄 שלב 2 – הוסף את כל הקבצים ששונו

```bash
git add .
```

הנקודה (`.`) אומרת לגיט להוסיף את כל הקבצים ששונו.

---

## 📝 שלב 3 – צור commit עם הודעה

```bash
git commit -m "עדכון פרונטאנד ריאקט"
```

רשום הודעה שמתארת מה שינית, לדוגמה:

```bash
git commit -m "הוספתי קבצי הסבר לתיקיית notes"
```

---

## 🚀 שלב 4 – שלח את העדכונים לגיטהאב

```bash
git push origin main
```

> אם זו הפעם הראשונה – תתבקש להתחבר לגיטהאב (OAuth).

---

## 🔄 שלב 5 – משוך עדכונים מהענן

אם אתה רואה הודעה:

> “Your branch is behind origin/main”

הרץ:

```bash
git pull origin main
```

---

## ⚠️ שלב 6 – תיקון אם תיקייה לא עולה (למשל notes)

אם Git לא מזהה תיקייה מסוימת, הרץ:

```bash
git rm -r --cached notes
git add notes
git commit -m "Force add notes folder"
git push
```

---

## 🧩 גרסה מקוצרת לשימוש יומיומי

```bash
git add .
git commit -m "Update"
git push
```

---

**💡 טיפ:** השתמש ב־`git status` בכל שלב כדי לראות אילו קבצים עוד לא עודכנו.
